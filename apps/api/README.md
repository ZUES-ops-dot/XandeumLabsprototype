# pNode API

Fastify-based REST API serving validator telemetry, reward history, and epoch analytics.

## Endpoints
- `GET /health` -- liveness probe
- `GET /validators/:id/metrics` -- time-series metrics
